# @google/model-viewer khai báo peer three@^0.183 nhưng dist đã bundle three riêng,
# trong khi project ghim three@0.171 cho react-three-fiber. Hai bên không đụng nhau ở runtime.
# legacy-peer-deps để npm install không fail vì peer-conflict advisory này.
legacy-peer-deps=true
